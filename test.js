const PromptSync = require('prompt-sync');

const prompt = require('prompt-sync')();
// criar uma função que recebe um array de numero e retorna um array com cada item x2
// [1,2,3] -> [2,4,6]

// criar uma função que recebe um array e um numero e retorna umarray so com os itens maiores que o numero
// [1,2,3], 2 -> [3]

function array(){
    var list = []
}

const run = array();